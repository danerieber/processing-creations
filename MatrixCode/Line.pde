public class Line {
  public float x, y, len;
  public float speed;
  
  public Line(float x, float y, float len, float speed) {
    this.x = x;
    this.y = y;
    this.len = len;
    this.speed = speed;
  }
}